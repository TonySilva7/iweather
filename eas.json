{
  "cli": {
    "version": ">= 7.6.2"
  },
  "build": {
    "preview": { // usado para testes
      "android": {
        "distribution": "internal"
      },
      "ios": {
        "simulator": true
      }
      // "env": {}, // foi mudado para "secrets" no expo.dev
    },
    "production": { // usado para produção
      // "env": {}, // foi mudado para "secrets" no expo.dev
    }
  }
}
